import styled from 'styled-components';
import { Container as C, Loader as L } from '../Loader/styled';

export const Container = styled(C)`
  position: relative;
  top: initial;
  right: initial;
  width: 178px;
  height: 178px;
  background: white;
`;

export const Loader = styled(L)``;
